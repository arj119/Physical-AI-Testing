# Foundry OSDK + integration
physical-ai-qa-cell-sdk>=0.5.0
requests>=2.28.0
python-dotenv>=1.0.0

# Hardware drivers
opencv-python-headless>=4.8.0
pymycobot>=3.6.0,<5.0.0
numpy>=1.24.0
Pillow>=10.0.0

# ONNX inference (development / CI — install for local model testing)
onnxruntime>=1.16.0

# Jetson-specific (pre-installed on JetPack or manual install)
# torch  — use Jetson PyTorch wheel from NVIDIA
# tensorrt  — pre-installed with JetPack
# pycuda  — for TensorRT inference

# Testing
pytest>=7.0.0
